C C-LB+o-mb-o+o-mb-o.litmus
{
}

{
#include "api.h"
}

P0(int *x0, int *x1)
{
	int r2;

	r2 = READ_ONCE(*x1);
	smp_store_release(x0, 2);
}


P1(int *x0, int *x1)
{
	int r2;

	r2 = smp_load_acquire(x0);
	WRITE_ONCE(*x1, 2);
}

exists (1:r2=1 /\ 0:r2=1)
