C C-WWC+o+o-data-o+o-addr-o

{
x=z;
b=a;
}

void P0(int **x)
{
	WRITE_ONCE(*x, x);
}

void P1(int **x, int **y)
{
	r1 = READ_ONCE(*x);
	smp_store_release(y, r1);
}

void P2(int **x, int **y)
{
	r2 = READ_ONCE(*y);
	WRITE_ONCE(*r2, a);
}

exists(1:r1=x /\ 2:r2=x /\ x=x)
